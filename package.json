{
  "name": "@theantitwink/update-package-version",
  "author": "The Antitwink™️",
  "license": "MIT",
  "version": "0.0.3",
  "devDependencies": {
    "root": "github:actions/toolkit",
    "@vercel/ncc": "latest",
     "typescript": "^4.4.2"
  },
  "description": "Updates an npm package.json file to match a version number",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build ./src/index.ts "
  },
  "keywords": [],
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  }
}
